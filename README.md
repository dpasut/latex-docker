This repo. should allow you to build my LaTeX stuff, which isn't always overly-easy.

# Usage
Do a `cp -n * <target-dir>`, then run `./build.sh <target .tex file>` inside `<target-dir>`. Should work.
